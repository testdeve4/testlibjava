plugins {
    id 'java'
}

java {
    modularity.inferModulePath = true
}

group = 'com.github.username' // Replace with your GitHub username
version = '1.0' // Match the Git tag

repositories {
    mavenCentral()
}

dependencies {
    // Add dependencies if needed
}
